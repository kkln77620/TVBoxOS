# 临时代办问题清单

> 记录用户反馈的待处理问题，处理完成一项勾选一项。

## 问题 1
- [x] 设置界面按钮布局优化（等分铺满 + 固定列间距 + 顶部间距） — 已完成，v1.0.20260806_1252

## 问题 2
- [x] **缓存不可用** — 已修复（待安装验证）
  - 根因：缓存 URL 未解析/无鉴权 headers；m3u8 加密分片无法合并；协议地址无法直接下载
  - 已实施：
    1. 播放页自动缓存——播放成功即后台全速下载本集（复用真实播放 URL+headers，静默入队）
    2. DownloadManager 支持 headers 透传（UA/Referer/Cookie 与播放一致）
    3. m3u8 AES-128 加密分片解密（#EXT-X-KEY 解析 + CBC 解密）
    4. 协议地址（noprotocol: 等）拒绝直下：详情页提示走播放自动缓存，DownloadManager 拦截非 http/https
    5. TipDialog 确认按钮补 dismiss（删除配置确认后对话框不消失）
  - 第二轮修复（本轮）：
    6. 净化 m3u8 走本地代理 127.0.0.1:port/m3u8，自动缓存此前排除 127.0.0.1 导致净化源全部跳过 —— 已修复：自动缓存支持净化代理 m3u8
    7. App 重启后恢复的"下载中"任务无下载线程，永久卡死且阻塞同 URL 重新入队 —— 已修复：重启恢复时标记失败，允许重新入队
    8. 同 URL 失败任务重复堆积 —— 已修复：重新入队时清理历史失败任务
    9. m3u8 内容严格校验 #EXTM3U 头（净化代理可能返回空内容）
  - 第三轮修复（虚拟后台播放缓存，待验证）：
    10. 详情页缓存按钮：直链媒体地址直接下载；协议地址/解析接口走"虚拟后台播放"——后台静默执行播放解析（SourceViewModel.getPlay，不渲染画面），拿真实直链+鉴权 headers 后入队下载
    11. 自动缓存 headers 复用：切集等空 headers 路径沿用最近一次播放鉴权参数（修复防盗链源 404）
    12. 下载失败原因附带任务 URL（如 HTTP404 | url），便于定位
  - 第四轮修复（三合一地址/无法播放，待验证）：
    13. TVBox 三合一地址（真实URL@播放方式@名称@集数）自动清理 @ 后缀再下载，修复 404
    14. m3u8 分片合并输出 .ts 扩展名（原 .mp4 内容实为 TS 流导致无法播放）
    15. 下载完成后文件头探测：内容无效（HTML/防盗链页）标失败并给出原因；TS/mp4 扩展名自动修正
    16. m3u8 进度总量固定估算，不再上下浮动

## 问题 3
- [x] **设置页添加"联系作者"** — 已完成（待安装验证）
  - 系统设置页独占整行按钮（layout + layout-v21 同步）
  - 点击弹二级菜单显示 QQ:3921435329，点击复制到剪贴板（ContactAuthorDialog）
